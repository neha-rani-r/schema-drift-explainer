# Copy to .env.local and fill in your values
# Never commit .env.local to git

# Your Cloudflare Worker URL (deployed from worker.js)
VITE_WORKER_URL=https://schema-drift-proxy.YOUR_SUBDOMAIN.workers.dev
